@model EForumKLTN.ViewModels.HomeVM

@{
    ViewData["Title"] = "Trang chủ E-Forum";
}

<div class="container-fluid py-5">
    
</div>

<div class="container-fluid vesitable pb-5">
    <div class="container pb-5">
        <h2 class="display-6 text-primary mb-4">Sản phẩm nổi bật</h2>
        <div class="owl-carousel vegetable-carousel">
            @foreach (var item in Model.FeaturedProducts)
            {
                @await Html.PartialAsync("_CardProduct", item)
            }
        </div>

        <div class="row">
            <div class="col-12">
                <a asp-action="Index" asp-controller="HangHoa" class="fw-bold d-inline-flex align-items-center" style="color: #81bd00; font-size: 1.1rem; transition: 0.3s;">
                    Xem tất cả sản phẩm <i class="fas fa-arrow-right ms-2"></i>
                </a>
            </div>
        </div>

    </div>
</div>